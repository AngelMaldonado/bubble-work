/dist/
*.db
.DS_Store
/bubble

# frontend toolchain — the built bundle (web/dist) IS committed so the Go build
# needs no JS toolchain; only the dev artifacts are ignored.
/frontend/node_modules/
/frontend/.vite-dist/
